<?php

    // Define a class named 'Stock'
    class Stock
    {
        // Declare a public property named 'symbol' and initialize it to NULL
        public $symbol = NULL;

        // Declare a public property named 'name' and initialize it to NULL
        public $name = NULL;

        // Declare a public property named 'price' and initialize it to NULL
        public $price = NULL;

        // Declare a public property named 'time' and initialize it to NULL
        public $time = NULL;

        // Declare a public property named 'change' and initialize it to NULL
        public $change = NULL;

        // Declare a public property named 'open' and initialize it to NULL
        public $open = NULL;

        // Declare a public property named 'high' and initialize it to NULL
        public $high = NULL;

        // Declare a public property named 'low' and initialize it to NULL
        public $low = NULL;

        // Declare a public property named 'day' and initialize it to NULL
        public $day = NULL;
    }

?>